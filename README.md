Small e-commerce prototype used in my technical interview.

* **Tech**: React 19, Redux Toolkit, React Router 6, Vite.
* **Data**: DummyJSON `/products` API (comes with thumbnails & prices).
* **Features**: Category filter, client-side pagination, cart with Redux, login/logout with JWT stored locally.

Run locally:

```bash
npm install
npm run dev